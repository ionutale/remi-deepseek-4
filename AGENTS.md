## Agent skills

### Issue tracker

Issues are tracked in a single `issues.md` file at the repo root. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses default label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo (one CONTEXT.md + docs/adr/ at the root). See `docs/agents/domain.md`.
